# This file configures static analysis for the TradePilot AI Flutter app.

include: package:flutter_lints/flutter.yaml

linter:
  rules:
    # The repository parameter intentionally remains public while the stored
    # field is private, so an initializing formal is not appropriate here.
    prefer_initializing_formals: false